import React from 'react';
import ReactLoading from 'react-loading';

const Loading = () => {
    return <ReactLoading type="spinningBubbles" color="red" height={'12%'} width={'12%'} />;
};

export default Loading;
